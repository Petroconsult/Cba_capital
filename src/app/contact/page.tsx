import ContactForm from "@/components/ui/ContactForm";

export default function ContactPage() {
  return (
    <main className="min-h-screen bg-[var(--slate-bg)] flex items-center justify-center py-24 px-6">
      <ContactForm />
    </main>
  );
}